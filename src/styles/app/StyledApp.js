import styled from '@emotion/styled/macro';

const StyledApp = styled.div`
  position: relative;
  width: 100vw;
  min-width: 360px;
  height: 100vh;
  overflow: hidden;
`;

export default StyledApp;
